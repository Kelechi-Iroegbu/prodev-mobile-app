// app/(home)/saved.tsx
import { View, Text } from "react-native";

const Saved = () => {
  return (
      <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
        <Text style={{ fontSize: 24 }}>Saved Screen</Text>
      </View>
  )
}

export default Saved;